{
  "name": "homebridge-zwave-direct",
  "version": "1.0.0",
  "description": "Homebridge plugin that talks directly to a OpenZWave USB stick",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "tsc",
    "start": "node_modules/.bin/homebridge -P ."
  },
  "author": "Mario Smit",
  "license": "MIT",
  "keywords": [
    "homebridge-plugin",
    "zwave",
    "homekit"
  ],
  "engines": {
    "node": ">=7.7.2",
    "homebridge": ">=0.2.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.47"
  },
  "dependencies": {
    "openzwave-shared": "^1.4.0"
  }
}
